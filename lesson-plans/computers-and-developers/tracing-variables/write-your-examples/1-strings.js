'use strict';

/* Strings

TODO: create a few variables(strings):
- you need to add comments to show where the variable been
declared, initialized, read , reassigned,...
*/

// declaration variable "temp" & initialisation it as a string
let temp = ` `;

// declaration variable "a" & initialisation it as a string
let a = `red`;

// declaration variable "b" & initialisation it as a string
let b = `black`;

// declaration variable "c" & initialisation it as a string
let c = ` & `;

// concatination of the strings
temp = a + c + b;

//output value "temp" to the browser's console
console.log(temp);
