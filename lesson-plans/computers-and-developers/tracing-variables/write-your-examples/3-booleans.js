'use strict';

/* Booleans

TODO: create a few variables(boolean):
- you need to add comments to show where the variable been
declared, initialized, read , reassigned,...
*/

// declaration variable  & initialisation it as a boolean value
let isSmart = false;

//read condition
if (isSmart) {
  //output value to the browser's console
  console.log(`You can play poker`);
  //if previous is false - read condition
} else {
  //output value to the browser's console
  console.log(`You shouldn't play poker, you'll lose all your money!!!`);
}
